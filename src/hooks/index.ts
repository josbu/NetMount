/**
 * Hooks - 自定义 Hooks 统一导出
 *
 * 包含：
 * - useNotification: 通知
 */

export { useNotification } from './useNotification'
export type {
  WithNotificationOptions,
  UseNotificationReturn,
} from './useNotification'
