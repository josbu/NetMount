/**
 * Components - 组件库统一导出
 *
 * 包含：
 * - ErrorBoundary: 错误边界组件
 */

export {
  ErrorBoundary,
} from './ErrorBoundary'
export type {
  ErrorBoundaryProps,
  ErrorBoundaryState,
} from './ErrorBoundary'
